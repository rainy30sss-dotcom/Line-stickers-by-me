import streamlit as st

st.set_page_config(page_title="LINE Sticker Generator", page_icon="🎨")

st.title("🎨 LINE Sticker Generator AI")
st.subheader("สร้างสติกเกอร์ของคุณเองได้ทุกที่")

# ส่วนรับข้อมูล
sticker_text = st.text_input("ระบุข้อความที่จะใส่ในสติกเกอร์", placeholder="เช่น 'ได้ค่าาา'")
mood = st.selectbox("เลือกโทนอารมณ์", ("สดใส (Bright)", "กวนๆ (Funny)", "น่ารัก (Cute)"))

if st.button("🚀 เริ่มสร้างสติกเกอร์"):
    if sticker_text:
        st.balloons()
        st.success(f"กำลังจำลองการสร้างสติกเกอร์: '{sticker_text}' โทน {mood}")
        # ในอนาคตเราจะเอาโค้ด GEN รูปมาใส่ตรงนี้ครับ
    else:
        st.warning("กรุณาระบุข้อความก่อนครับ")
